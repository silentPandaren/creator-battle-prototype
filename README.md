# Creator Battle — Техническая документация

## Описание проекта

**Creator Battle** — это прототип веб-приложения для проведения соревнований между блоггерами (пилотами) в игре War Robots. Приложение отображает рейтинг команд, топ донатеров и ленту покупок в реальном времени.

### Основной функционал

- **Визуализация команд**: Отображение 2, 3 или 4 команд с их очками
- **Рейтинг команд**: Динамический расчет позиций на основе набранных очков
- **Топ-5 донатеров**: Список самых активных донатеров в каждой команде
- **Лента покупок**: Отображение последних покупок в реальном времени
- **Таблица призов**: Динамическое отображение призов в зависимости от количества команд
- **Debug панель**: Инструменты для тестирования и симуляции покупок

## Структура проекта

```
creator-battle-prototype/
├── index.html          # Основной HTML файл
├── styles.css          # Все стили (CSS)
├── script.js           # Вся логика приложения (JavaScript)
├── war-robots-logo.png # Логотип игры
└── README.md           # Документация
```

## Быстрый старт

### Локальный запуск

1. **Клонируйте репозиторий**:
   ```bash
   git clone https://github.com/silentPandaren/creator-battle-prototype.git
   cd creator-battle-prototype
   ```

2. **Откройте в браузере**:
   - Просто откройте файл `index.html` в браузере
   - Или используйте локальный сервер:
     ```bash
     # Python 3
     python -m http.server 8000
     
     # Node.js (npx)
     npx serve
     
     # PHP
     php -S localhost:8000
     ```

3. **Откройте в браузере**: `http://localhost:8000`

### Требования

- Современный браузер с поддержкой ES6+ (Chrome, Firefox, Safari, Edge)
- Локальный веб-сервер (опционально, для работы с CORS)

## Архитектура

### Структура файлов

#### `index.html`
Содержит HTML-разметку страницы:
- Шапка сайта с навигацией
- Блок события с командами
- Модальное окно "About"
- Debug панель

#### `styles.css`
Содержит все стили приложения:
- **Design tokens**: Цвета, отступы, типографика из MY.GAMES Market
- **Компоненты**: Стили для карточек команд, ленты, модальных окон
- **Адаптивность**: Медиа-запросы для планшетов и мобильных устройств

#### `script.js`
Содержит всю логику приложения:
- **Данные**: Константы блоггеров, призов, цветов команд
- **Инициализация**: Функции создания и управления командами
- **Рендеринг**: Функции отрисовки UI компонентов
- **Контролы**: Обработчики событий для debug панели

## Основные компоненты

### 1. Система команд

Команды создаются динамически на основе массива `BLOGGERS`:

```javascript
const BLOGGERS = [
  { name: 'PilotStorm', code: 'STORM', avatar: '...' },
  { name: 'PilotVortex', code: 'VORTEX', avatar: '...' },
  // ...
];
```

Каждая команда содержит:
- `blogger`: Информация о блоггере (пилоте)
- `score`: Общее количество очков команды
- `donators`: Map с донатерами и их суммами

### 2. Система очков

Очки начисляются при покупке:
- Каждая покупка добавляет очки команде
- Очки суммируются для каждого донатера
- Рейтинг команд пересчитывается автоматически

### 3. Визуализация

#### Score Bar (Баланс сил)
Горизонтальная полоса, показывающая процентное соотношение очков команд.

#### Team Cards (Карточки команд)
Отображают:
- Аватар и имя пилота
- Код команды
- Текущую позицию в рейтинге
- Общее количество очков
- Топ-5 донатеров

#### Feed (Лента покупок)
Отображает последние 12 покупок с:
- Именем покупателя
- Кодом команды
- Суммой покупки

#### Prizes (Призы)
Динамически отображает призы в зависимости от количества команд (2, 3 или 4).

## API функций

### Инициализация

```javascript
initTeams(count)
```
Инициализирует указанное количество команд (2, 3 или 4).

### Покупки

```javascript
addPurchase(teamIdx, buyerName, amount)
```
Добавляет покупку для указанной команды:
- `teamIdx`: Индекс команды (0-3)
- `buyerName`: Имя покупателя
- `amount`: Сумма в points

### Рендеринг

```javascript
render()
```
Перерисовывает все компоненты интерфейса.

```javascript
renderScoreBar()
renderTeams()
renderFeed()
renderPrizes()
```
Отдельные функции для рендеринга каждого компонента.

### Утилиты

```javascript
formatNumber(n)
```
Форматирует число с разделителями тысяч (ru-RU формат).

```javascript
getTop5(donators)
```
Возвращает топ-5 донатеров из Map.

## Debug панель

Debug панель находится в правом нижнем углу и позволяет:

1. **Изменять количество команд**: Кнопки 2, 3, 4
2. **Симулировать покупки**:
   - Ввести имя покупателя (или оставить пустым для случайного)
   - Указать сумму в points
   - Выбрать команду из списка
   - Нажать "💸 Purchase"
3. **Автоматический режим**: Включить чекбокс "Auto mode" для случайных покупок каждые 800ms

### Скрытие панели

Нажмите на заголовок "🛠 Debug Panel" для сворачивания/разворачивания.

## Design System

Проект использует design tokens из MY.GAMES Market:

### Цвета

- **Команды**: `--team1` (красный), `--team2` (синий), `--team3` (зеленый), `--team4` (желтый)
- **Фон**: `--colorBgLayout`, `--colorBgContainer`
- **Текст**: `--colorText`, `--colorTextSecondary`, `--colorTextTertiary`
- **Границы**: `--colorBorder`, `--colorBorderSecondary`

### Отступы

Используются переменные `--base4`, `--base8`, `--base12`, `--base16`, `--base24`, `--base32` и т.д.

### Типографика

- **Шрифт**: Inter (Google Fonts)
- **Веса**: 450 (Regular), 550 (Medium), 600 (Lightbold), 650 (Semibold), 700 (Bold)

## Адаптивность

### Планшеты (max-width: 999px)
- Команды (3-4) отображаются в 2 колонки
- Заголовок события перестраивается вертикально

### Мобильные (max-width: 499px)
- Все команды в 1 колонку
- Уменьшенные отступы
- Адаптивная навигация с горизонтальной прокруткой
- Уменьшенные размеры шрифтов

## Модальное окно "About"

Открывается кнопкой "ℹ️ About" в заголовке события.

Содержит информацию:
- Что такое Creator Battle
- Как это работает
- Призы
- Правила

Закрытие:
- Клик по overlay (фону)
- Кнопка "✕"
- Клавиша Escape

## Кастомизация

### Изменение блоггеров

Отредактируйте массив `BLOGGERS` в `script.js`:

```javascript
const BLOGGERS = [
  { 
    name: 'PilotName', 
    code: 'CODE', 
    avatar: 'URL_к_аватару' 
  },
  // ...
];
```

### Изменение призов

Отредактируйте массив `PRIZES` в `script.js`:

```javascript
const PRIZES = [
  { place: '1', value: 500 },
  { place: '2', value: 400 },
  // ...
];
```

### Изменение цветов команд

Отредактируйте переменные в `styles.css`:

```css
:root {
  --team1: #e84057;  /* Красный */
  --team2: #3079ff;  /* Синий */
  --team3: #0fc31e;  /* Зеленый */
  --team4: #f5a623;  /* Желтый */
}
```

## Интеграция с бэкендом

Для интеграции с реальным бэкендом необходимо:

1. **Заменить моковые данные** на API вызовы
2. **Добавить WebSocket** для обновлений в реальном времени
3. **Реализовать аутентификацию** пользователей
4. **Добавить обработку ошибок** и состояния загрузки

### Документация API

Полная структура API описана в файле [API.md](./API.md), включая:
- Все endpoints для работы с событиями
- WebSocket для обновлений в реальном времени
- Структуру моделей данных
- Примеры использования
- Обработку ошибок и безопасность

### Пример интеграции API

```javascript
// Получение данных события
async function fetchEventData(eventId) {
  const response = await fetch(`/api/battles/${eventId}`);
  const data = await response.json();
  return data;
}

// Получение рейтинга команд
async function fetchLeaderboard(eventId) {
  const response = await fetch(`/api/battles/${eventId}/leaderboard`);
  const data = await response.json();
  return data;
}

// WebSocket для обновлений в реальном времени
const ws = new WebSocket(`ws://api.example.com/ws/battles/${eventId}/`);
ws.onmessage = (event) => {
  const message = JSON.parse(event.data);
  if (message.type === 'leaderboard_update') {
    updateLeaderboard(message.data.teams);
  }
};
```

## Браузерная совместимость

- ✅ Chrome 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Edge 90+

Требуется поддержка:
- ES6+ (const, let, arrow functions, Map)
- CSS Custom Properties (переменные)
- Flexbox и Grid

## Производительность

- Лента покупок ограничена 50 элементами (последние 12 отображаются)
- Рендеринг происходит только при изменении данных
- CSS анимации используют `transform` и `opacity` для GPU-ускорения

## Безопасность

⚠️ **Важно**: Это прототип для демонстрации. Для production необходимо:

- Валидация всех пользовательских данных
- Защита от XSS (санитизация HTML)
- CSRF защита для API запросов
- Rate limiting для API endpoints

## Лицензия

Прототип создан для внутреннего использования MY.GAMES.

## Контакты

Вопросы и предложения: создайте issue в репозитории.

---

**Версия**: 1.0.0  
**Последнее обновление**: 2025

